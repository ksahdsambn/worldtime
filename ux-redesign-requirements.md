# WorldTime 易用性改造与首页 SEO 区优化 —— 需求改造文档

> 状态：待评审
> 日期：2026-08-27
> 范围：首页（时钟 / 排期两个核心模式）与首页底部 SEO 文字区
> 原则依据：仓库 AGENTS.md 设计语境（清晰优先、温暖但不拖慢、克制密度、无障碍默认、全球化设计、玻璃只用于 chrome 不用于正文）

---

## 一、背景与问题诊断

### 1.1 产品现状

WorldTime 首页目前为一页两态结构：顶部玻璃导航栏（品牌、城市搜索、UTC 时钟，以及语言、帮助、设置、主题等入口），其下是一个体积很小的分段切换控件，在「时钟」（城市时间卡片列表）与「重叠时段」（7 天 × 24 小时排期网格）两种模式之间切换；页面底部是一块 SEO 文字区，包含一段简介、热门城市链接列表（24 条）与时差对照链接列表（46 条），全部平铺陈列、无任何折叠。

### 1.2 问题一：核心功能入口晦涩，新手看不懂"能干什么"

- **模式命名使用领域术语。**「重叠时段（overlap）」是排期领域的黑话，首次访问的用户无法在几秒内建立"输入城市 → 看到共同空闲 → 选出开会时间"的任务预期，违背"对首次访客立即易懂"的设计原则。
- **入口体积与功能权重不匹配。** 产品最核心的两（或三）个能力，被压缩成工作条左侧的两个小按钮，价值主张（跨时区找共同时间）没有任何大块、显性的表达。
- **答案不前置。** 排期模式已具备"推荐时段"能力，但它藏在工具栏的一个弹窗里，用户仍需自己读懂热力图网格才能得出结论；新手的学习成本主要压在"读图"这一步。
- **工具栏控件过密。** 排期视图工具栏同时陈列图例、推荐时段、一天/七天切换、周翻页、回到现在共五组控件，新手第一眼无法分辨主操作（拖选时段）与次要操作。
- **引导路径不连贯。** 空状态引导已存在，但从"加完城市"到"切换排期视图"再到"拖选分享"的完整任务链缺少显性的下一步提示。

### 1.3 问题二：底部 SEO 文字区占用过多视觉面积

- 简介段落加两组链接（共 70 条）全部平铺，无折叠、无分栏；从页面截图看，约占可见区域近一半（约 45%）的纵向空间。
- 面积高的根源不是文字量（中文简介仅约 80 字），而是 70 条链接以自动换行、无固定列数的松散流式排布陈列，行距与区块间距偏松，且三个小节纵向堆叠。
- 该区域承担搜索排名（SEO 内链权重传递）与 AI 检索引用（GEO）两项职责，不能简单删除或移走，只能在"内容保留、视觉收缩"的前提下优化。

---

## 二、改造目标与非目标

### 2.1 目标

1. **新手五秒可懂**：首次访客不借助任何说明，即可通过页面上的大块任务入口看懂产品能做哪几件事，并一键选定任务开始（选卡片 → 添加城市 → 自动进入对应模式）。
2. **结论先行**：排期场景下，用户先看到系统给出的推荐结论（哪段时间对大家都合适），再决定是否手动微调，而非被迫先学会读网格。
3. **底部区域面积大幅收缩**：目标压缩为"一句实体定义 + 两三行紧凑链接 + 一个折叠条"的量级（纵向高度约为现状的五分之一到三分之一），且 SEO 内链一条不少、GEO 可引用内容不丢。
4. **全程无障碍与多语言**：所有新增交互满足 WCAG 2.1 AA（键盘可达、焦点可见、不以颜色为唯一信息载体），11 个语言环境同步交付，布局容忍文本膨胀与非拉丁文字。

### 2.2 非目标（明确不做）

- 不改变 URL 状态同步、分享链接、键盘快捷键等既有专家路径行为。
- 不引入第三方 UI 组件库，继续沿用项目内自研控件模式。
- 不改动 `/about`、`/faq`、城市页、国家页、时差换算页等既有 SEO 落地页的内容策略（仅首页底部区调整）。
- 不在本次范围内更换品牌色板、字体体系或玻璃质感体系。

---

## 三、改造方案一：核心功能卡片化

### 3.1 总体思路

把"从工具视角组织的界面"翻译为"从任务视角组织的界面"：用户不需要理解什么是时钟模式、什么是重叠模式，只需要回答"我想干什么"。具体由四项相互配合的改动组成：任务卡片入口、文案去术语化、结论前置、渐进式披露。另需明确：时钟模式的城市时间卡纵向列表本身就是"卡片式信息流"的既有形态，本次完全保留，只做文案与层级增强——卡片化改造针对的是"入口层"（让功能被看见），不推翻已有的信息流呈现。

### 3.2 需求一：任务卡片入口

**需求描述**

在用户尚无城市（首用/空状态）时，提供大块的任务卡片入口，作为产品的"门面"；每张卡片对应一个核心任务。

**卡片规划（2~3 张）**

- 卡片一：「看看各地现在几点」——进入时钟模式（查看多地当前时间与昼夜状态）。
- 卡片二：「找个大家都有空的时间」——进入排期网格（对比一周内各城市工作/睡眠时段，选出共同空闲并分享）。
- 卡片三（可选，且当前存在去向缺口）：「换算两个城市的时差」——项目目前只有具体城市对的换算落地页（如"北京 ↔ 纽约"），没有通用换算工具页；若保留此卡片，需同时新增"先选两个城市、再进入对应对照页"的轻量入口，否则本期砍掉此卡片、只保留两张。

**每张卡片的信息结构**

- 一个功能图标（延续现有图标体系与品牌蓝主色）；
- 一句任务式大标题（动词开头、口语化、不带术语）；
- 一句补充说明（这个任务完成后的收益，例如"找到对所有人都合适的时间段"）；
- 明确的可点击整体区域，键盘可达且有可见焦点。

**交互规则**

- 点击任务卡片即选定"当前任务"：随即高亮顶部城市搜索，引导先添加城市；添加第一个城市后自动进入所选任务对应的模式。当前界面在无城市时始终停在空状态、不区分模式，因此必须由这条引导流衔接，避免"点了卡片却仍停在原处"。
- 空状态中既有的快捷开始按钮（本地时区起步、预设城市组）必须保留，与任务卡片并存：卡片负责选任务，快捷按钮负责最快补城市；未选任务而直接点快捷按钮时，维持现状进入默认的时钟模式。
- 当用户已添加城市时，不再展示占屏的入口卡片，任务切换完全由第二期改写后的模式切换控件承担，不另设重复入口。
- 通过分享链接带选区参数直达排期视图的老路径行为完全不变，不经过入口卡片。

**视觉要求**

- 卡片区遵守"玻璃只用于 chrome"原则：卡片为不透明表面，使用现有表面色与描边令牌，不施加毛玻璃。
- 卡片在窄屏纵向堆叠、宽屏横向排列（两张或三张均需成立），容忍俄语、越南语等较长文案的换行，不出现截断或溢出。
- 保持"克制密度"：卡片数量不超过三张，说明文字各不超过一句。

### 3.3 需求二：全局文案去术语化

**需求描述**

对两个模式的命名及其周边控件文案做一次"普通话化"清洗，覆盖 11 个语言包。

**命名建议方向（最终文案以文案评审为准）**

- 时钟模式：「时钟」可保留，或改为更任务化的「各地时间」；
- 排期模式：「重叠时段」改为「找共同时间」或「会议排期」；
- 时间控制条、网格工具栏内的控件标签（如「设定时间」「1 天 / 7 天」等既有标签）逐个评审口语化，确保每个标签单独读出来都能被非专业用户理解。

**执行要求**

- 仅改文案键的值，不改键结构，避免波及引用关系；
- 所有语言包由翻译流程统一产出，不逐个手写译文；
- 改动后跑一遍多语言截图走查，重点检查模式切换控件在最长语言下的宽度表现。

### 3.4 需求三：推荐时段结论前置

**需求描述**

把现有"推荐时段弹窗"的数据升级为排期网格上方的醒目结论卡，实现"先给答案、再给画布"。

**行为细节**

- 有明确推荐时，结论卡以一句自然语言给出最强结论，例如"看起来周二 21:00 对所有人都合适"，并附带"选中这段"的主操作按钮，点击即在网格上落选区；
- 结论卡沿用推荐弹窗的现有门槛：仅在城市数达到两个及以上时出现（单一城市不存在"共同时间"概念），不满足时该区域不占位；
- 若推荐时段不在当前网格视野内（例如处于一天视图或翻到了其它周），点击主操作需自动切回七天视图并定位到推荐所在日期，再落选区；
- 存在多个候选时，结论卡给出首选并允许横向切换查看次选（次选以紧凑形式陈列）；
- 无任何可用推荐（例如参与城市横跨过多时区、任何候选时刻总有一方处于深夜）时，结论卡如实说明冲突原因并引导调整参与城市或放宽时段，而不是隐藏自己；
- 结论卡不遮挡网格，位于工具栏与网格之间，且不参与玻璃效果（内容性表面保持不透明）；
- 原弹窗入口可保留为高级用户的次要入口，也可在结论卡稳定后移除（评审时二选一）。

**文案要求**

- 结论语句式遵循品牌"友好、温暖"的语调（如"看起来是个不错的时间"），但绝不以牺牲信息精度为代价；
- 11 种语言同步提供结论句式的完整翻译。

### 3.5 需求四：渐进式披露，收拢次要控件

**需求描述**

重排排期视图工具栏的信息层级，主操作唯一且显性，次要控件收纳。

**具体安排**

- 主层级保留：时段图例（读图必需的语义钥匙）与结论卡；
- 次层级收纳：一天/七天切换、周翻页、回到现在合并进一个"更多"或"视图选项"的收纳控件（沿用现有自研弹出菜单模式）；
- 新用户首次进入排期视图时，出现一次性的轻量提示，说明"在网格上横向拖动即可框选时间段"，看过即不再出现（记忆在本地存储）；
- 收纳不得破坏键盘可达性：被收纳的控件仍需能通过键盘展开后到达，焦点顺序合理。

### 3.6 方案一涉及范围（供排期评估）

- 首页工作区组件（模式切换与视图装配）；
- 首用空状态组件（升级为任务卡片入口，保留既有快捷开始按钮；如保留第三张卡片，含"选两个城市"轻量入口的实现）；
- 排期视图工具栏与推荐时段组件（结论前置与控件收纳）；
- 全部 11 个语言包中与模式命名、工具栏、推荐结论相关的文案键；
- 相关交互测试与多语言视觉走查。

---

## 四、改造方案二：首页底部 SEO 文字区优化

### 4.1 总体思路

内容留在页面文档里供搜索引擎与 AI 抓取，视觉上通过"压缩可见层 + 折叠长尾 + 多列分栏"三件事收缩面积。机器可读的事实性信息继续由既有结构化数据兜底。

### 4.2 需求一：简介压缩为一句实体定义

- 保留一句包含核心关键词的定义句（"世界时钟、时区转换器、会议安排工具"三个实体必须出现），作为可见层的第一行；
- 定义句是 AI 生成引擎最常引用的句式，压缩时不得删除实体与限定词，只砍冗余修饰；
- 更完整的介绍不在首页重复展开，由"关于"页承接（该页已有功能与场景介绍，无需新增内容）。

### 4.3 需求二：链接分区改造（分栏 + 分层可见）

**热门城市链接**

- 最重要的 10~12 个城市以紧凑的标签样式在可见层直接陈列；
- 其余城市移入折叠区。

**时差对照链接（46 条，面积大头）**

- 整体移入折叠区，可见层只留该分区的标题与折叠开关。

**布局要求**

- 可见层链接采用多列紧凑网格（窄屏两列、宽屏四列左右），以标签化小体积呈现，行距收紧；
- 折叠开关使用原生可折叠区块元素，默认收起，展开后内部同样使用多列网格；
- 折叠区内容位于页面文档内部（客户端无需任何请求即可展开），保证抓取完整性；
- 折叠交互满足无障碍要求：开关可键盘操作、展开状态可被读屏感知。

### 4.4 需求三：结构化数据与 GEO 兜底

- 维持现有三个结构化数据块（网页应用、组织、站点）不缺项；描述字段与新的可见层定义句同源，但允许保留比可见句更完整的表述，避免压缩可见层连带削弱机器可读的语义；
- 常见问答的结构化数据继续留在"常见问题"页，不在首页重复；
- 可选增强：评估增加面向 AI 爬虫的站点说明文件（llms.txt 一类），作为低成本的 GEO 补充，评审时决定是否纳入本期。

### 4.5 SEO / GEO 保障措施（风险对冲）

- **对冲"隐藏内容降权"**：折叠区内均为内链导航性质内容，同时把最有价值的少量链接保留在可见层，即使折叠内容被降权，核心内链权重传递不受影响；
- **索引完整性验证**：改造上线后用搜索引擎的抓取渲染工具确认折叠内容进入渲染后文档；用站点检索指令抽查长尾链接页是否维持收录；
- **GEO 验证**：抽查主流 AI 检索产品对站点定义句的引用是否维持，作为上线后观察项而非发布阻塞项。

### 4.6 方案二涉及范围（供排期评估）

- 仅首页文件中底部区块的结构调整，不触碰站点页脚（版权与法务链接行）；
- 语言包新增少量折叠开关的标题文案键（如"更多城市""全部时差对照"），11 种语言同步；
- 既有文案键的改动仅限简介定义句的压缩，其余既有键不动；数据源不变（城市与对照清单维持现有来源）。

---

## 五、实施步骤（分四期，先做确定性高的改动，再推进交互改造）

### 第一期：底部 SEO 区收缩（小而确定，先行落地）

1. 重排底部区块为"定义句 + 可见标签层 + 折叠长尾层"三段式；
2. 可见层与折叠层均改多列紧凑网格；
3. 压缩各语言的简介定义句、新增折叠开关文案键，一并完成 11 语言翻译；
4. 核对结构化数据描述字段语义完整（与定义句同源、允许更详细，不因压缩丢失实体信息）；
5. 验收：多语言截图走查无溢出；纵向高度达到收缩目标；上线后按第 4.5 节完成抓取渲染与收录复核。

### 第二期：模式与控件文案去术语化（纯文案，低风险）

1. 评审敲定两个模式的最终命名与工具栏各控件标签文案；
2. 更新 11 个语言包（走统一翻译流程）；
3. 验收：最长语言下模式切换控件与工具栏不换行错位；读屏朗读可理解。

### 第三期：任务卡片入口

1. 设计并实现首用/空状态下的任务卡片（含可选第三张卡片；若保留第三张，先落地"选两个城市"的轻量入口）；
2. 点击卡片后的任务引导流：聚焦城市搜索，添加第一个城市后自动进入所选模式；
3. 与模式切换控件的衔接：已有城市时的任务切换完全由改写后的模式切换控件承担，不新增重复入口；
4. 验收：分享链接直达行为不回归；键盘可完成"选定任务 → 添加城市 → 进入模式 → 切换任务"全流程；浅色与深色两种主题下视觉走查通过。

### 第四期：结论前置与工具栏收纳

1. 推荐结论卡（含多候选切换与无推荐时的冲突说明）；
2. 工具栏次要控件收纳与一次性拖选提示；
3. 验收：结论卡与网格在不同城市数量、不同语言下不遮挡不溢出；收纳后控件键盘可达；既有选区与分享流程回归通过。

每期独立提交、独立可回滚；一至二期互不依赖，三、四期依赖第二期的新文案。

---

## 六、风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| 折叠内容被搜索引擎降权 | 长尾内链效果减弱 | 重要链接保留可见层；上线后监测收录与抓取统计 |
| 文案改动波及 11 语言 | 工作量膨胀、术语不一致 | 走统一翻译流程并附术语表；键结构不变 |
| 老用户找不到原入口 | 熟练效率下降 | 模式切换控件保留原位；URL 直达与快捷键不动 |
| 结论卡给出错误推荐 | 信任受损 | 推荐逻辑复用既有算法不新写；冲突场景必须显性说明而非隐藏 |
| 卡片文案在俄语/越南语下过长 | 布局破坏 | 以最长语言文案做设计基准走查 |

---

## 七、整体验收标准

1. 首次访客可用性走查：五位未接触过产品的用户中，至少四位能在五秒内说出"能干什么"，并在一分钟内完成"选定任务 → 添加两个城市 → 看到推荐结论"的路径。
2. 底部区域纵向高度较改造前减少约七成，全部内链仍存在于页面文档中。
3. 搜索渲染抓取确认折叠内容完整可索引；结构化数据校验通过。
4. 全量回归：分享链接直达、选区拖拽、键盘快捷键、主题切换、语言切换全部通过。
5. 无障碍抽检：新增卡片、折叠开关、结论卡均键盘可达、焦点可见、读屏可理解。
