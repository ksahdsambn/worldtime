# WorldTime 易用性改造实施执行文档

> 依据文档：ux-redesign-requirements.md（下称"需求文档"）
> 性质：逐步执行计划，步骤可逐条勾选销项
> 冲突处理：执行中发现与需求文档不一致时，以需求文档为准，先修订需求文档再继续执行
> 禁止事项：本工程全程不引入第三方界面组件库，不改 URL 状态与分享参数行为，不改键盘快捷键，不改其它既有落地页（关于、常见问题、城市页、国家页、换算页）的内容策略，不动品牌色板、字体与玻璃质感体系

---

## 一、执行总则（每一期都必须遵守）

1. **分期与顺序**：共四期。第一期与第二期互不依赖、可并行或任意先后；第三期、第四期必须在第二期完成之后进行。
2. **提交与回滚**：每期在独立分支上开发，验收全部通过后才合并；合并后如发现问题，按期独立回滚，不牵连其它期。
3. **每期通用收尾**（缺一不可）：
   - 全量回归五项：分享链接直达排期视图、网格拖选、键盘快捷键、主题切换、语言切换；
   - 浅色与深色两种主题下的截图走查；
   - 至少覆盖三种语言的走查，其中必须包含文案最长的语言（俄语或越南语任选其一）；
   - 更新或补充本期改动对应的交互测试用例，随本期一并提交；
   - 对照本期"验收"小节逐条确认并记录结果。
4. **翻译纪律**：所有新增或修改的界面文案，一律走项目既有的统一翻译流程并附术语表，禁止手工逐语言编写译文。
5. **布局基准**：一切新布局先用最长的目标语言文案验证，不得出现截断、溢出或换行错位。
6. **无障碍纪律**：每个新交互控件都须满足 WCAG 2.1 AA 的基本要求——键盘可操作、焦点可见、读屏可理解；不以颜色作为唯一信息载体。

---

## 二、开工前必须关闭的决策点

以下决策未关闭前，对应步骤不得开工。每项决策需在评审中二选一（或明确取舍）并记录结论。

| 编号 | 决策点 | 影响的阶段 | 选项 | 建议默认 |
| --- | --- | --- | --- | --- |
| D1 | 时钟模式的最终命名 | 第二期、第三期 | 保留「时钟」／改为「各地时间」 | 保留「时钟」 |
| D2 | 排期模式的最终命名 | 第二期、第三期 | 「找共同时间」／「会议排期」 | 「找共同时间」 |
| D3 | 第三张任务卡片（时差换算）去留 | 第三期 | 砍掉只留两张／保留并新增"选两个城市"轻量入口 | 砍掉（本期省一件事） |
| D4 | 原推荐时段弹窗去留 | 第四期 | 保留为次要入口／结论卡稳定后移除 | 先保留，观察后再定 |
| D5 | 面向 AI 爬虫的站点说明文件是否纳入本期 | 不阻塞任何一期 | 纳入／仅评估 | 仅评估 |

---

## 三、第一期：底部 SEO 区收缩（小而确定，先行落地）

**目标**：底部区域改为"一句定义句 + 少量可见标签 + 折叠长尾"三段式，纵向高度较改造前约降七成，70 条内链一条不少且全部留在页面文档内。

### 步骤

1. **记录改造前基线**：
   - 以固定视口宽度截取改造前底部区域的截图（至少中文、英文、俄语三种语言 × 浅深两主题），留存为对照物；
   - 记录改造前的纵向高度数值，作为"约降七成"的验收基数；
   - 导出当前底部区域全部内链清单（24 条城市链接 + 46 条换算链接，共 70 条），作为"一条不少"的核对底册。
2. **确认可见层城市名单**：按现有热门清单顺序取前 10~12 个城市作为可见层名单，交产品确认后冻结；其余城市进折叠区。
3. **准备文案**：
   - 以母版语言将简介压缩为一句实体定义句：三个实体（世界时钟、时区转换器、会议安排工具）与关键限定词必须保留，只删冗余修饰；
   - 新增折叠开关的标题文案（如"更多城市""全部时差对照"）；
   - 以上文案连同术语表一并走统一翻译流程，产出十一种语言版本；压缩与翻译均不得丢失实体信息。
4. **结构重排**：将首页底部区块改为三段式——定义句段（一行）、可见链接层（紧凑标签）、折叠长尾层（默认收起）；可见层与折叠层内部均采用多列紧凑网格（窄屏约两列、宽屏约四列），标签化小体积、行距收紧。
5. **链接分层落位**：
   - 可见层：冻结名单内的城市标签；
   - 折叠区一：其余城市链接；
   - 折叠区二：46 条换算链接整体收纳，可见层只保留该分区标题与折叠开关。
6. **折叠交互实现**：使用浏览器原生的可折叠区块能力，默认收起；开关可键盘操作，展开状态可被读屏感知；折叠内容直接写在页面文档里，不依赖任何脚本请求即可展开。
7. **结构化数据核对**：确认页面既有的三个结构化数据块（网页应用、组织、站点）一项不缺；其描述内容与新的定义句同源、允许更完整，确保压缩可见层没有削弱机器可读的实体信息；常见问答的结构化数据继续留在"常见问题"页，不在首页重复。
8. **无障碍自查**：仅用键盘完成"聚焦折叠开关—展开—遍历链接—收起"全流程，焦点顺序合理、焦点可见。
9. **本地验证**：
   - 在禁用浏览器脚本的条件下重新加载首页，确认 70 条链接仍然完整存在于页面中（证明内容在文档内、不依赖脚本渲染）；
   - 对照基线截图做双主题、多语言对比，确认无溢出、无换行错位。
10. **验收与合并**：纵向高度达到收缩目标、走查全部通过后合并本期。

### 验收清单

- 多语言截图走查无溢出；
- 纵向高度较基线约降七成；
- 70 条内链一条不少，且禁用脚本后仍完整可见；
- 结构化数据校验通过、三项不缺。

### 上线后跟进（不阻塞合并）

- 用搜索引擎的抓取渲染工具确认折叠内容进入渲染后文档；
- 用站点检索指令抽查长尾换算页是否维持收录；
- 记录复核结果；如个别长尾页出现收录异常，先将相关链接提升至可见层或解除对应折叠，调整后再观察一个周期，仍异常才考虑回滚本期布局。

---

## 四、第二期：模式与控件文案去术语化（纯文案，低风险）

**目标**：两个模式及全部相关控件的标签口语化，十一种语言同步，仅改文案取值、不改任何文案键结构。

### 步骤

1. **召开文案评审**：
   - 关闭决策点 D1、D2，敲定两个模式的最终命名；
   - 对下列标签逐个过审：模式切换两个按钮、时间控制条（如「设定时间」「回到现在」）、排期工具栏（推荐入口、「1 天 / 7 天」、向前/向后、图例各色含义）、时钟卡片的既有文案；空状态文案不在本期处理，留待第三期随任务卡片一并定稿；
   - 评审标准：每个标签单独朗读出来，非专业用户也能明白其含义；符合品牌友好、温暖的语调。
2. **更新语言包**：按评审结论更新十一种语言的对应文案值，走统一翻译流程并附术语表；除简介定义句（第一期已处理）外，不新增、不删除、不改名任何文案键。
3. **验收与合并**：通过后合并。

### 验收清单

- 文案最长的语言下，模式切换控件与工具栏不换行、不溢出；
- 读屏朗读抽检通过（模式名、控件名可理解）；
- 全量回归五项通过（本次为纯文案变更，回归确认无行为意外变化）。

---

## 五、第三期：任务卡片入口（依赖第二期完成）

**目标**：首用/空状态升级为任务卡片门面，与既有快捷开始按钮并存；新手路径"选卡片 → 加城市 → 自动进入所选模式"全通。边界：时钟模式既有的城市时间卡信息流完全保留，本期只改造"入口层"（空状态与任务选择），不推翻已有的信息流呈现。

### 步骤

1. **设计定稿**：
   - 关闭决策点 D3；若保留第三张卡片，先设计并排期"选两个城市"轻量入口，否则本期只做两张；
   - 出卡片视觉稿：每张含一个功能图标（沿用现有图标体系与品牌蓝）、一句动词开头的任务式标题、一句收益说明；整体区域可点击；卡片数量不超过三张，说明文字各不超过一句；
   - 出空状态整体布局稿：任务卡片区与既有快捷开始按钮（本地时区起步、预设城市组）的并存关系必须画清楚——卡片负责选任务，快捷按钮负责最快补城市。
2. **实现任务卡片**：改造首用空状态区域，卡片为不透明表面，沿用现有的表面色与描边令牌（不施加玻璃效果），窄屏纵向堆叠、宽屏横向排列，两张与三张布局均需成立。
3. **实现交互规则**：
   - 点击卡片即选定任务：高亮顶部城市搜索，引导添加城市；添加第一个城市后自动进入所选任务对应的模式；
   - 快捷开始按钮原样保留；未选任务而直接点快捷按钮时，维持现状进入默认时钟模式；
   - 用户已有城市后，不再展示入口卡片，任务切换完全由第二期改写后的模式切换控件承担，不新增任何重复入口；
   - 通过分享链接带选区参数直达排期视图的路径行为保持不变、不经过卡片。
4. **无障碍实现**：卡片为可聚焦控件，回车即触发，焦点可见；卡片标题使用页面标题层级中合适的级别，不打乱文档大纲。
5. **验收与合并**：通过后合并。

### 验收清单

- 仅用键盘可完成"选定任务 → 添加城市 → 进入模式 → 切换任务"全流程；
- 浅色与深色两种主题下视觉走查通过；
- 分享链接直达行为回归通过；
- 长文案语言下卡片无截断、无溢出。

---

## 六、第四期：结论前置与工具栏收纳（依赖第二期完成）

**目标**：排期视图"先给答案再给画布"；工具栏主次分明；新手一次性拖选提示。

### 步骤

1. **关闭决策点 D4**：确定原推荐弹窗去留，并按结论执行。
2. **实现推荐结论卡**（推荐计算复用既有算法，不新写）：
   - 门槛：城市数达到两个及以上才出现，否则该区域不占位；
   - 有明确推荐时：一句自然语言给出最强结论（如"看起来周二 21:00 对所有人都合适"），附"选中这段"主操作，点击即在网格上落下选区；
   - 推荐时段不在当前视野（一天视图或翻到其它周）时：点击主操作自动切回七天视图并定位到推荐所在日期再落选区；
   - 多个候选时：给出首选，次选以紧凑形式陈列、可切换查看；
   - 无任何可用推荐时：如实说明冲突原因（如参与城市跨时区过多、任何时刻总有一方处于深夜），并引导调整参与城市或放宽时段，不隐藏该区域；
   - 位置：排期工具栏与网格之间，不遮挡网格；内容性表面保持不透明，不参与玻璃效果。
3. **实现工具栏收纳**：
   - 主层级只留：时段图例（读图的语义钥匙）与结论卡；
   - 将「1 天 / 7 天」切换、周翻页、「回到现在」合并收进一个"更多"或"视图选项"收纳控件，沿用项目自研弹出菜单模式；
   - 被收纳控件保持键盘可达：可展开、可遍历、焦点顺序合理。
4. **实现一次性拖选提示**：新用户首次进入排期视图时出现轻量提示（说明"在网格上横向拖动即可框选时间段"），关闭或读过后不再出现，记忆存于本地。
5. **准备结论文案**：结论句式、多候选切换、冲突说明等全部新文案，按品牌友好、温暖的语调编写母语版本，但不得牺牲信息精度，连同术语表走统一翻译流程产出十一种语言。
6. **验收与合并**：通过后合并。

### 验收清单

- 结论卡在不同城市数量（1、2、5、10 个城市）与不同语言下不遮挡、不溢出；
- 单城市时结论卡正确不占位；无推荐场景正确显示冲突说明；
- 收纳后所有控件键盘可达；一次性提示只出现一次；
- 既有选区拖选与分享流程回归通过。

---

## 七、上线后观察任务（全部非阻塞，按周跟踪）

1. 折叠内容索引状态：抓取渲染结果、长尾换算页收录抽查（第一期产出的观察项）。
2. GEO 观察：抽查主流 AI 检索产品对站点定义句的引用是否维持。
3. 决策点 D5 落地：若评审结论为"纳入本期"，在第四期完成后作为补充任务实施站点说明文件，并补入第九节覆盖对照表销项；若为"仅评估"，记录评估结论即可，不阻塞本计划。
4. 如任一观察项出现异常：收录类异常按第一期"上线后跟进"的递进补救路径处理，其余按需求文档第六节风险对策处置，必要时局部回滚对应期。

---

## 八、总验收（对照需求文档第七节，全部通过即工程完结）

1. 可用性走查：五位未接触过产品的用户中，至少四位能在五秒内说出"能干什么"，并在一分钟内完成"选定任务 → 添加两个城市 → 看到推荐结论"。
2. 底部区域纵向高度较改造前约降七成，70 条内链仍全部存在于页面文档中。
3. 搜索渲染抓取确认折叠内容完整可索引；结构化数据校验通过。
4. 全量回归：分享链接直达、选区拖拽、键盘快捷键、主题切换、语言切换全部通过。
5. 无障碍抽检：任务卡片、快捷按钮、折叠开关、结论卡、收纳菜单、一次性提示均键盘可达、焦点可见、读屏可理解。

---

## 九、需求覆盖对照表（执行完成后逐行销项）

| 需求文档条目 | 对应执行步骤 |
| --- | --- |
| 3.2 任务卡片入口（卡片规划、信息结构、交互规则、视觉要求） | 五.1、五.2、五.3、五.4 |
| 3.3 全局文案去术语化 | 四.1、四.2 |
| 3.4 推荐时段结论前置（门槛、结论、定位、多候选、冲突说明、位置、弹窗去留、文案要求） | 六.1、六.2、六.5 |
| 3.5 渐进式披露（层级、收纳、一次性提示、键盘） | 六.3、六.4 |
| 3.6 方案一涉及范围（工作区与空状态组件、文案键、交互测试与多语言走查） | 五各步、四各步、一.3 |
| 4.2 简介压缩为一句实体定义 | 三.3 |
| 4.3 链接分区改造（分层、多列、折叠、无障碍） | 三.2、三.4、三.5、三.6、三.8 |
| 4.4 结构化数据与 GEO 兜底 | 三.7；D5 见七.3 |
| 4.5 SEO / GEO 保障与验证 | 三.1（底册）、三.9（禁脚本验证）、七.1、七.2 |
| 4.6 方案二涉及范围（仅首页底部、文案键改动边界、数据源不变） | 三各步（沿用现有热门清单与换算清单）、三.7 |
| 2.1 目标 1~4 | 五、四、六（目标 1、2）；三（目标 3）；一.3、一.5、一.6（目标 4） |
| 2.2 非目标 | 本文档开头禁止事项（含不动其它落地页与品牌体系） |
| 六、风险与对策 | 七.4（异常处置）、一.4（翻译纪律）、一.5（长文案基准） |
| 七、整体验收标准 | 八 |

---

## 十、回滚要点

- **第一期回滚**：恢复底部三段式为原平铺布局即可；新增的折叠标题文案键留在语言包中无副作用，不必清除。
- **第二期回滚**：按提交记录恢复各语言文案取值；因未改键结构，恢复为逐值还原。
- **第三期回滚**：移除任务卡片区，恢复空状态原布局；交互规则改动随组件一并回退，快捷开始按钮本就未被改动。
- **第四期回滚**：结论卡与工具栏收纳整体回退；若 D4 曾执行"移除弹窗"，回滚时一并恢复弹窗。
- 任一期回滚后，重跑该期验收清单与全量回归五项，确认回到已知的良好状态。
