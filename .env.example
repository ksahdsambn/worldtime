# Google OAuth Client ID —— 纯前端 GIS Token Client 方案（需求 6.1 日历叠加）。
# 从 Google Cloud Console → APIs & Services → Credentials → OAuth client ID 获取。
# 重要：纯前端方案【只用 Client ID】，不需要、也不应使用 Client Secret。
# 必须带 NEXT_PUBLIC_ 前缀，使其内联进客户端 bundle（浏览器要用）。
# 同时记得在 OAuth client 的 "Authorized JavaScript origins" 里加上站点域名与 http://localhost:3000。
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# 站点根 URL（SEO：metadataBase / canonical / sitemap / robots）。
# 生产环境务必改为真实域名。
NEXT_PUBLIC_SITE_URL=https://time.eqde.de
