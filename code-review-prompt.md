# WorldTime 项目全面代码审查提示词（3 轮递进）

## 你的角色

你是一名资深的全栈代码审查工程师，擅长 TypeScript / React / Next.js / 时区计算 / 前端安全与性能优化。你将以**独立、客观、严格**的标准对本项目进行全面审查。

## 审查目标

对 WorldTime 项目代码进行**3 轮递进式**审查，覆盖 Bug 与逻辑正确性、安全漏洞、性能、i18n/可访问性/代码质量四个维度，找出所有需要修复或改进的问题，并产出一份结构化的独立审查报告。

**重要：这是一次全新独立审查。不要假设代码已经过充分审查，也不要参考项目中任何历史审查记录（如 progress.md）。以批判性视角从零开始。**

## 第一步：理解项目（审查前必做）

在开始审查前，先用 30 分钟通读以下内容，建立完整心智模型：

1. **产品定位**：阅读 `markdown/REQUIREMENTS.md`，理解 WorldTime 是什么——一个纯客户端（无后端、无账号、无追踪）的世界时钟 + 时区转换器 + 会议安排器，数据存在浏览器 localStorage 和 URL 中。
2. **技术栈**：Next.js 14（App Router，Node server 模式，standalone 输出）+ React 18 + TypeScript 5（strict）+ Tailwind CSS + Zustand 5 + Luxon 2.5 + next-intl 4.13（11 语言）+ Vitest 4。
3. **目录结构**：`src/lib/`（15 个纯函数业务逻辑模块）、`src/components/`（25 个组件）、`src/store/`（Zustand）、`src/data/`（1203 城市 / 2362 节假日 / 国家元数据）、`src/app/[locale]/`（App Router 页面）、`messages/`（11 个语言 JSON）、`tests/`（单元测试）。
4. **状态架构**：理解状态存在三处并需保持同步——Zustand store（运行时）、localStorage（持久化）、URL query（分享）。重点关注同步逻辑是否有遗漏或竞态。
5. **关键算法**：`lib/time.ts`（DST 检测/偏移量）、`lib/grid.ts`（小时列构建、DST 去重）、`lib/heatmap.ts`（"最差状态优先"着色 + 周末/节假日覆盖）、`lib/calendar.ts`（ICS 生成/RFC5545 转义/base64 事件码）、`lib/shareUrl.ts`（URL 状态编解码）、`lib/sun.ts`（日出日落天文计算）。

理解后，列出你心中的"高风险区域"清单，作为后续审查的重点。

---

## 审查维度与检查清单

### 维度一：Bug 与逻辑正确性（核心重点）

这是 WorldTime 最关键的质量维度——时间计算错误会让产品完全失效。逐项核查：

**时区与 DST（重点中的重点）**
- [ ] 所有时区转换是否正确使用 IANA 时区（如 `Asia/Shanghai`），而非固定偏移
- [ ] DST 切换边界：春进/秋退当天的处理是否正确（秋退有重复小时，春进有跳过小时）
- [ ] `nextDSTChange` 探测逻辑：是否在本地正午探测以避开午夜附近的边界问题
- [ ] `buildColumns` 中 DST 重复列的去重逻辑是否覆盖所有情况
- [ ] 跨时区比较时间时，基准时区（homeId）的选择是否一致
- [ ] Luxon 的 `setZone`、`toISO`、`offset` 用法是否正确，是否混淆 local time / UTC
- [ ] 历史日期：是否依赖当前规则计算过去的偏移量（Olson 数据库应覆盖，但要确认）
- [ ] 南半球 DST（10-4 月夏令时）是否与北半球同样处理正确

**逻辑正确性**
- [ ] 边界输入：空城市列表、单城市、城市列表超长时的行为
- [ ] off-by-one 错误：循环边界、数组索引、日期加减
- [ ] `Date.now()` 的重复调用：同一渲染周期内多次调用 `Date.now()` 可能导致不一致，是否在合适处缓存
- [ ] `isWeekendAt` 的周末规则（中东 [5,6] vs 中国 [6,0] vs 其他）是否按国家正确应用
- [ ] 节假日数据与周末的优先级覆盖关系
- [ ] 状态同步：Zustand store 改变后，localStorage 写入和 URL 更新是否都触发、是否有顺序依赖
- [ ] URL 状态解码：非法/过期/不完整 query 参数的降级处理
- [ ] 浮点数精度：偏移量（如 5.5、5.75、5.25）的比较是否用整数毫秒而非浮点小时
- [ ] 选择范围（selection）的起止时间归一化

**异常处理**
- [ ] try/catch 是否吞掉了应上报的错误
- [ ] 异步操作的错误路径（SW 注册失败、clipboard 拒绝、localStorage 配额超限）
- [ ] 空值/undefined 的防御：optional chaining 与 nullish coalescing 使用是否充分

### 维度二：安全漏洞

尽管是纯客户端应用，攻击面较小，仍需严格核查：

**注入类**
- [ ] XSS：搜索 `dangerouslySetInnerHTML`、`eval(`、`new Function(`、`innerHTML =`、`document.write`——应为零
- [ ] URL 解析：`shareUrl.ts` 的 `decodeState` 是否对所有字段做正则校验、未知 city id 是否被过滤
- [ ] `parseSlug`（landing 页）：`--` 分隔符解析是否会被恶意 slug 绕过（构造歧义分隔）
- [ ] base64 事件码 `decodeEventCode`：填充规范化、JSON 解析后的结构校验是否充分、畸形输入是否安全失败
- [ ] 跳转链接：生成的 Google Calendar / mailto / ICS URL 是否对参数做了 URL 编码，防止注入额外参数

**持久化与隔离**
- [ ] localStorage 读取：恢复时是否重新校验 placeId 是否仍存在于城市数据中（防止过期/篡改数据）
- [ ] localStorage 解析：try/catch 是否覆盖 JSON.parse 失败
- [ ] 不同 key 之间是否有读写串扰

**Service Worker**
- [ ] `public/sw.js`：缓存键版本管理、激活时旧缓存清理是否彻底
- [ ] 同源守卫 `url.origin !== self.location.origin` 是否覆盖所有 fetch 请求
- [ ] 缓存是否可能泄漏跨会话的用户状态（URL 中的选择/光标被缓存进 HTML）

**依赖与构建**
- [ ] `package.json` 依赖版本是否有已知 CVE（可用 `npm audit`）
- [ ] Dockerfile 是否以非 root 用户运行
- [ ] `.dockerignore` 是否排除了敏感文件
- [ ] 构建是否泄露源码或 sourcemap 到生产

### 维度三：性能

**渲染与状态**
- [ ] 大列表渲染：1203 个城市的搜索结果、节假日列表是否做了虚拟化或分页
- [ ] `useMemo` / `useCallback` / `React.memo`：昂贵的派生计算（heatmap、grid 列、日出日落）是否被记忆化
- [ ] 不必要的重渲染：Zustand selector 是否精细（避免订阅整个 store）、context 是否分层
- [ ] `Date.now()` 驱动的定时器：更新频率是否过高（如每秒重渲染整个 grid）、是否限制了更新范围
- [ ] key prop：列表项 key 是否稳定（避免用数组索引）

**资源与包体积**
- [ ] cities.ts（1203 行）/ holidays.ts（660 行）是否全量打包到客户端 bundle，是否可按需加载
- [ ] 动态 import：非首屏组件（如导出、设置面板）是否懒加载
- [ ] 图片/字体：是否预加载关键资源
- [ ] html-to-image、dnd-kit 等较重依赖是否只在需要时加载

**SSR/SSG/ISR**
- [ ] `time-converter/[slug]` 的 `generateStaticParams` + `revalidate` 配置是否合理
- [ ] 是否有 hydration mismatch 风险（客户端时间 vs 服务端渲染时间）
- [ ] 首屏关键路径的阻塞资源

### 维度四：i18n / 可访问性 / 代码质量

**i18n（11 语言）**
- [ ] 硬编码文案：搜索 `src/` 中是否有未走 `useTranslations` 的中英文字面量
- [ ] key 齐全性：对比 11 个 `messages/*.json`，是否每个 key 在所有语言都存在、无拼写不一致
- [ ] 动态拼接：是否有 `t('prefix') + variable + t('suffix')` 这种破坏语序的拼接（不同语言语序不同）
- [ ] 复数/性别：是否考虑了需要复数形式的语言
- [ ] 日期/数字格式化：是否走 Intl 而非硬编码格式
- [ ] 文案长度溢出：德语/俄语等长语言在 UI 中是否会撑破布局

**可访问性（a11y）**
- [ ] ARIA：combobox（CitySearch）、dialog、tabs 等 ARIA 角色/状态是否完整
- [ ] 键盘可达：所有交互是否可用键盘完成（拖拽是否有键盘替代、Tab 顺序是否合理）
- [ ] 焦点管理：对话框/面板打开关闭时的焦点捕获与归还
- [ ] 颜色对比度：heatmap 红/橙/绿、day/night 配色在深色模式下的可读性
- [ ] 屏幕阅读器：图标按钮是否有 `aria-label`、时间信息是否有文本替代
- [ ] 缩放：页面在 200% 缩放下是否可用

**代码质量**
- [ ] TypeScript：是否有 `any`、`as` 类型断言滥用、`@ts-ignore`、未使用的变量
- [ ] 死代码：未被引用的导出函数/组件/常量
- [ ] 重复代码：可提取的重复模式
- [ ] 命名：是否清晰一致
- [ ] 测试覆盖盲区：`tests/` 只覆盖纯函数，组件/状态逻辑/URL 同步无测试——识别关键未测试路径

---

## 审查流程（3 轮递进）

### 第 1 轮：全局快速扫描（广度优先）

**目标**：用最快速度识别明显问题，建立问题清单的"第一稿"，不追求深入。

**方法**：
1. 通读全部配置文件：`next.config.mjs`、`tsconfig.json`、`package.json`、`Dockerfile`、`docker-compose.yml`、`vitest.config.mts`、`.eslintrc.json`、`tailwind.config.ts`。
2. 对 `src/` 全部文件执行关键词扫描（用 grep/Grep）：
   - 安全：`dangerouslySetInnerHTML`、`eval`、`innerHTML`、`document.write`、`new Function`
   - 代码异味：`TODO`、`FIXME`、`@ts-ignore`、`any`、`console.log`
   - i18n：在 `src/components/` 和 `src/app/` 中搜索中文字面量（未走 i18n 的硬编码）
3. 浏览每个 `src/lib/` 模块的导出函数签名和顶层注释，识别可疑设计。
4. 快速浏览所有 `messages/*.json`，人工/脚本核对 key 齐全性。
5. 检查测试目录，列出哪些模块完全没有测试。

**退出标准**：产出一份"候选问题清单"（粗粒度，可能含误报）。进入第 2 轮前**不要**急于下结论，标记为"待验证"。

**输出**：在第 1 轮结束时，把候选问题写入报告草稿的"第 1 轮发现"小节。

---

### 第 2 轮：逐函数深度跟踪（深度优先）

**目标**：对第 1 轮的候选问题逐一验证，并对高风险模块逐函数追踪数据流，找出深层 Bug 与漏洞。

**方法**：
1. **按模块逐个深入**，每个模块回答："输入有哪些边界？数据如何流动？哪里可能出错？"重点模块顺序：
   - `lib/time.ts` → DST / 时区 / 格式化（追每个公开函数，列举 DST 边界用例）
   - `lib/grid.ts` → `buildColumns` / `isWeekendAt`（构造跨时区 7 天网格的完整数据流）
   - `lib/heatmap.ts` → `columnColor` 算法（"最差优先"优先级的所有组合）
   - `lib/calendar.ts` → ICS / base64 / 日期格式（RFC5545 合规性、转义完整性）
   - `lib/shareUrl.ts` → 编解码（往返一致性、非法输入）
   - `lib/sun.ts` → 天文计算边界（极夜/极昼、高纬度）
   - `store/useWorldTimeStore.ts` → 状态机一致性、action 副作用
   - `lib/useUrlState.ts` / `useLocalPersist.ts` / `useNow.ts` → 三处状态同步的竞态
2. **构造具体测试用例**（在脑中或用 node/luxon 实际运行验证），尤其针对：
   - DST 切换当天（春进跳过、秋退重复）
   - 跨日期变更线的城市
   - 半小时偏移时区（印度 5.5、尼泊尔 5.75）
   - 空/单元素输入
   - 畸形 URL query
3. **组件层**：重点审查 `PlacesPanel.tsx`、`TimeGrid.tsx`、`SelectionBar.tsx`、`CitySearch.tsx` 的事件处理、effect 依赖、重渲染范围、内存泄漏（定时器/监听器清理）。
4. **安全复核**：手动追踪从 URL query → store → 渲染 的完整路径，确认无注入；追踪 localStorage → store → 渲染 路径。
5. **性能复核**：识别每次 `Date.now()` tick 触发重渲染的组件树范围；检查大列表是否有虚拟化。

**退出标准**：每个候选问题要么确认为真 Bug（附复现）、要么标记为误报（附理由）。新增第 1 轮未覆盖的深层发现。

**输出**：把验证后的发现写入"第 2 轮发现"小节，每条附文件:行号、复现/触发条件、影响。

---

### 第 3 轮：交叉验证 + 回归确认 + 优先级汇总

**目标**：消除误报、查漏补缺、形成最终可执行的报告。

**方法**：
1. **误报剔除**：重新审视第 1、2 轮每一条发现，确认：
   - 是否真的会触发？（是否被上游校验拦截、是否被 React 默认行为保护）
   - 是否与项目设计意图冲突？（区分"Bug"与"有意取舍"）
2. **查漏补缺**：
   - 模拟 3-5 条典型用户操作路径，端到端走一遍代码，看是否有未发现的问题（例如：添加城市 → 拖拽排序 → 切换时区基准 → 导出 ICS → 分享 URL → 在另一设备打开）。
   - 重新审视 4 个维度的检查清单，逐项打勾确认是否都已覆盖，未覆盖的补查。
3. **回归风险评估**：对每条建议的修复方案，预判是否会引入新问题（例如改 DST 逻辑是否会影响 heatmap）。
4. **优先级排序**：按下面的严重度定义给所有确认问题分级，P0/P1 优先列出。

**退出标准**：每条发现都是"经过验证、确认为真、可复现、有明确修复方向"的。报告无重复、无矛盾。

**输出**：完成最终报告，把第 3 轮新增/调整写入"第 3 轮发现"小节，并在报告末尾给出"汇总与优先级"表。

---

## 输出要求

### 报告文件

产出一份独立的 Markdown 文件：**`code-review-report.md`**（放在项目根目录）。**不要修改 `progress.md` 或任何已有文件**，这是一份全新的独立报告。

### 报告结构

```markdown
# WorldTime 代码审查报告

- 审查人：[AI 名称/版本]
- 审查日期：[日期]
- 审查范围：[列出审查的目录/文件]
- 声明：本次为独立审查，未参考项目内任何历史审查记录。

## 一、审查概述
[项目理解摘要、高风险区域识别、整体评价]

## 二、第 1 轮发现（全局快速扫描）
### [严重度标签] 发现标题
...

## 三、第 2 轮发现（逐函数深度跟踪）
### [严重度标签] 发现标题
...

## 四、第 3 轮发现（交叉验证与查漏补缺）
### [严重度标签] 发现标题
...

## 五、汇总与优先级
| 序号 | 严重度 | 类别 | 位置 | 简述 |
|------|--------|------|------|------|
| 1    | P0     | Bug  | lib/time.ts:142 | ... |

## 六、误报记录（第 3 轮剔除的候选问题）
[列出第 1/2 轮怀疑但第 3 轮判定为误报的条目，附剔除理由，体现严谨性]
```

### 单条发现格式

```markdown
### [P0/P1/P2/P3] 简明标题
- **严重度**：P0 / P1 / P2 / P3
- **类别**：Bug / 安全 / 性能 / i18n / a11y / 质量
- **位置**：`src/lib/xxx.ts:行号`（或组件名）
- **问题描述**：[一句话说清问题本质]
- **触发条件 / 复现**：[具体输入、操作步骤，或代码执行路径]
- **影响**：[对用户/数据/安全的实际后果]
- **建议修复**：[具体到代码层面的修改方向；如有多方案，列出权衡]
- **验证方式**：[如何确认修复有效——新增的测试用例或手动验证步骤]
```

### 严重度定义

| 级别 | 含义 | 示例 |
|------|------|------|
| **P0** | 阻塞性：会导致数据错误、崩溃、安全漏洞，必须立即修 | DST 计算错误导致时间显示错；XSS 漏洞 |
| **P1** | 严重：影响核心功能或有较高风险，应尽快修 | 状态同步丢失、URL 解析在畸形输入下崩溃 |
| **P2** | 一般：影响体验或可维护性，计划修复 | 性能热点、未处理的边界、测试缺失 |
| **P3** | 轻微/建议：优化项、代码质量、风格 | 死代码、命名、可读性 |

---

## 审查纪律（务必遵守）

1. **只审查，不修改代码**：本轮只产出报告，不直接改任何源文件（`code-review-report.md` 是唯一新增文件）。
2. **不臆测，要验证**：每条 Bug 发现必须有具体的触发条件或代码路径支撑，无法验证的"感觉有问题"归为 P3 疑问项而非结论。
3. **区分 Bug 与取舍**：项目有意为之的设计（如纯客户端、mock 的 Google OAuth）不算 Bug，但可在报告中作为"已知限制"列出。
4. **不盲目信任注释**：注释描述的意图可能与代码实现不符，以代码实际行为为准。
5. **复现优先**：对时间/时区类问题，尽量用 `node -e` 配合 Luxon 实际运行验证，给出可复现的命令。
6. **保持客观**：不要因为项目看起来"已经很完善"就放松标准，也不要为凑数量而夸大轻微问题。
7. **覆盖完整**：4 个维度的检查清单在第 3 轮必须逐项确认，未覆盖的项目要补查，不允许遗漏。
8. **报告可执行**：每条建议修复都要具体到"改哪个文件的哪段代码、改成什么样"，让后续开发者能直接动手。

---

开始审查。先完成"第一步：理解项目"，再依次执行 3 轮，最后产出 `code-review-report.md`。
